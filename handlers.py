@router.message(F.text == "Премиум 🚀")
async def premium_info(message: Message):
    t = get_texts(message.from_user.id)
    uid = message.from_user.id

    # Если премиум есть
    if has_active_premium(uid):
        until_ts, tariff = get_premium(uid)
        dt = datetime.fromtimestamp(until_ts).strftime("%d.%m.%Y")

        text = (
            f"🚀 У тебя активный Премиум!\n"
            f"Действует до: {dt}\n"
            f"Тариф: {tariff}\n\n"
            f"Пользуйся без ограничений ❤️"
        )
        await message.answer(text)
        return

    # Если премиума НЕТ — продаём красиво
    text = (
        "🚀 *Премиум-доступ ArtBazar AI*\n\n"
        "Получи неограниченный доступ к анализу товаров, ниш и рынков. "
        "Бот отвечает без лимита, быстрее и глубже.\n\n"
        "📌 *Тарифы:*\n"
        "• 1 месяц — 490 сом\n"
        "• 6 месяцев — 1990 сом\n"
        "• 1 год — 2990 сом\n\n"
        "🧑‍💼 Для подключения напиши менеджеру:\n"
        "👉 @Artbazar_support\n\n"
        "После оплаты премиум активируется сразу."
    )

    await message.answer(text, parse_mode="Markdown")
